var mongoose = require('mongoose');

var blogschema = new mongoose.Schema({
    blog-title: String,
    blog-text: String,
    created-date: {
	type: Date,
	"default": Date.now
    }
});

mongoose.model('My Blog' , blogschema);
